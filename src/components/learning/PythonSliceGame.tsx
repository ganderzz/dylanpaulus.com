import { useEffect, useState } from "preact/hooks";
import type { FunctionComponent } from "preact";

export const PythonSliceGame: FunctionComponent = () => {
    return (
        <div>
            
            <form>
                <input type="text" />
            </form>
        </div>
    );
}